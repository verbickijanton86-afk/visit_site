meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command: python server.py
  persistenceMount: /data
  containerPort: "3000"
  servicePort: "80"
